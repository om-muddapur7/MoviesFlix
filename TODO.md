# TODO: Fix "rnfes plugin not found" error (likely nativewind plugin issue)

- [ ] Clear Metro bundler cache
- [ ] Remove node_modules and reinstall dependencies
- [ ] Run the app to verify the error is resolved
